const AboutUs = () => (
  <div>
    interesting input from chatgpt on naming conventions. because page/ folders
    determine route names, you should use kebab case for routes like about-us
  </div>
);

export default AboutUs;
